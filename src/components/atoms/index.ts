// Atoms — smallest, presentational building blocks.
// Re-exports keep the existing file locations intact so the UI is not affected.
export { Logo } from "@/components/Logo";
export { NavLink } from "@/components/NavLink";
export { Reveal } from "@/components/Reveal";
